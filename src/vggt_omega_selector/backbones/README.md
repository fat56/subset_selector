# Backbones

Future wrappers for frozen VGGT-OMEGA, cheap image feature extractors, and cache manifests.

Do not add model code until a stage requires it. Stage 1 only needs enough wrapping to cache full-set/subset register readout embeddings.

