# Readouts

Future `RegisterReadoutHead` implementations.

Important constraints from the research report:

- VGGT-OMEGA does not provide a ready-made scene-level embedding for this project.
- Register token count must be read from config/checkpoint, not hard-coded.
- The readout target is geometric proxy alignment, not language alignment.

