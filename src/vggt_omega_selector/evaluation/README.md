# Evaluation

FastGS integration lives in `fastgs.py`. It checks the external checkout and
builds reproducible training commands for prepared Stage 1 subsets.

Stage 1 should produce scatter tables and Spearman/Pearson summaries before any selector training.
